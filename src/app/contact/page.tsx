import { memo } from "react";

const Contact = () => {
  return (
    <div>
      <h2 className="text-center text-2xl my-4 font-bold">Contact Page</h2>
    </div>
  );
};

export default memo(Contact);
